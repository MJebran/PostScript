




% /arg {exch def} def

% /square { 
%     0 dict begin   % dict is dictionary to make it local varibale instade of global
%     /side arg
%     % /startPx arg
%     % /startPy arg
%     newpath
%     200 200  moveto % starting point 
%     200 200 side add lineto % this is the left side of the box
%     200 side add 200 side add lineto % top side of the box
%     200 side add 200 lineto % right side of the box
%     % 200 200 lineto % tis is the bottom side of the box
    
%     fill
%     end % end of dict
%     } def

% /side 800 def
% side
% /startPx 200 def
% /startPy 200 def
% 0.75 setgray
% 100 square
% side
% 1 1 0 setrgbcolor
% 100 200 50 square
% side
% side

% stack
% showpage 